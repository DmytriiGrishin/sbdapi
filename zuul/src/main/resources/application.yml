server:
    port: 8888

spring:
  application:
    name: zuul

zuul:
    prefix: /api
    routes:
        forms:
            path: /forms/**
            serviceId: mongo-api

eureka:
 client:
  registryFetchIntervalSeconds: 30
  serviceUrl:
   defaultZone: ${EUREKA_SERV:http://138.68.140.160:8090/eureka/}